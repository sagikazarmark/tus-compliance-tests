# TODOs

Deferred by `/autoplan` review for `docs/superpowers/plans/2026-05-06-tus-compliance-coverage.md`.

## Tus Compliance Productization

- Add a standalone `tus-compliance run --base-url ...` CLI or equivalent packaged runner for arbitrary external tus servers.
- Add public conformance badges or hosted scorecards after the report schema stabilizes.
- Evaluate spec-generated tests from protocol clauses once the explicit Hurl/probe coverage audit is complete.
- Add arbitrary remote server target support that reuses the raw, unsupported, skipped, and active result model.
