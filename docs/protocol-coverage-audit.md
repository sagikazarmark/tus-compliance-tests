# tus Protocol Coverage Audit

This file maps each reviewed tus 1.0.0 coverage gap to spec evidence and planned or concrete test paths. Update `Coverage Added` from `Planned:` to concrete results as each implementation task lands.

| Finding | Spec Clause | Current Gap | Coverage Added | Verification |
|---|---|---|---|---|
| X-HTTP-Method-Override | Requests may use `X-HTTP-Method-Override` to override `POST`. | No method override coverage. | `tests/core/cp-override/cp-override-001-post-overrides-patch.hurl`, `tests/extensions/termination/ext-term-006-post-overrides-delete.hurl` | Dagger matrix; TUSD passed both active tests; tus-node-server core override skip is source-verified in `docs/server-noncompliance.md` |
| OPTIONS ignores Tus-Resumable | Server must ignore `Tus-Resumable` on `OPTIONS`. | No invalid-version OPTIONS check. | `tests/core/cp-opt/cp-opt-006-ignore-tus-resumable.hurl` | Dagger matrix; TUSD passed |
| Unsupported Tus-Resumable does not process or mutate | Unsupported protocol versions must be rejected and must not process upload state. | POST and PATCH side effects for unsupported versions are untested. | `tests/core/cp-ver/cp-ver-004-unsupported-post-no-location.hurl`, `tests/core/cp-ver/cp-ver-005-unsupported-patch-no-mutation.hurl` | Dagger matrix plus follow-up HEAD checks; TUSD, RUSTUS, and tus-node-server skips are source-verified in `docs/server-noncompliance.md` |
| Header integer grammar | `Upload-Offset` and `Upload-Length` must be non-negative integers. | Negative and non-integer header cases are incomplete. | `tests/core/cp-patch/cp-patch-011-upload-offset-negative.hurl`, `tests/core/cp-patch/cp-patch-012-upload-offset-non-integer.hurl`, `tests/core/cp-err/cp-err-003-upload-length-non-integer.hurl` | Dagger matrix attempted; new TUSD and tus-node active tests passed; RUSTUS Upload-Offset skips are source-verified in `docs/server-noncompliance.md` |
| Tus-Resumable response headers | Server responses, except `OPTIONS`, must include `Tus-Resumable`. | Success and error response header coverage is incomplete. | `tests/extensions/creation/ext-create-011-returns-tus-resumable.hurl`, `tests/extensions/termination/ext-term-007-returns-tus-resumable.hurl`, `tests/extensions/checksum/ext-csum-009-error-returns-tus-resumable.hurl` | Dagger matrix attempted; TUSD creation/termination passed; checksum and extension-specific response tests are filtered as unsupported when a server does not advertise the extension |
| Creation length/defer requirements | Creation requires `Upload-Length` or `Upload-Defer-Length`; defer value must be valid. | Missing required-header and invalid defer-value coverage. | `tests/extensions/creation/ext-create-012-requires-length-or-defer.hurl`, `tests/extensions/creation-defer-length/ext-defer-006-invalid-value-zero.hurl`, `tests/extensions/creation-defer-length/ext-defer-007-invalid-value-token.hurl` | Dagger matrix; TUSD active tests passed, RUSTUS and tus-node-server filtered as unsupported because they do not advertise creation |
| Metadata validation and exact echo | Metadata keys and values have grammar rules; HEAD should return stored metadata exactly. | Exact echo and invalid metadata key cases are missing. | `tests/extensions/creation/ext-create-013-metadata-exact-head.hurl`, `tests/extensions/creation/ext-create-014-metadata-empty-key-rejected.hurl`, `tests/extensions/creation/ext-create-015-metadata-comma-key-rejected.hurl` | Dagger matrix; TUSD active tests passed. Invalid-key cases are relaxed `201|400` sentinels because tus permits servers to ignore metadata and comma/empty-key cases are ambiguous after HTTP header parsing |
| OPTIONS optional headers and max size | `Tus-Version`, `Tus-Extension`, and `Tus-Max-Size` formats and enforcement must be validated when present. | Existing OPTIONS and max-size checks are smoke coverage only. | `tests/probes/options_headers.py`; existing `tests/extensions/creation/ext-create-007-exceeds-max-size.hurl` remains broad smoke coverage. | Dagger probe with conditional max-size enforcement when `creation` is advertised; probe is core-active and not filtered by the creation extension |
| Checksum header validation and no mutation | Malformed checksums must be rejected; unknown algorithms must not update upload offset. | Malformed formats and no-offset-update behavior are incomplete. | `tests/extensions/checksum/ext-csum-010-malformed-missing-space.hurl`, `tests/extensions/checksum/ext-csum-011-malformed-base64.hurl`, `tests/extensions/checksum/ext-csum-012-unknown-algorithm-no-offset-update.hurl` | Dagger matrix exported to `results/tusd-task5-checksum-final`, `results/rustus-task5-checksum-final`, and `results/tus-node-task5-checksum-final`; all status files returned `0`. New checksum files were included in `all-*` and filtered to `unsupported-*` where checksum was not advertised; no skips added. |
| Checksum trailer behavior | `checksum-trailer` must accept checksum data in an HTTP trailer when advertised. | Existing coverage only checks advertisement, not raw trailer behavior. | `tests/probes/checksum_trailer.py` | Dagger matrix exported to `results/tusd-task5-checksum-final`, `results/rustus-task5-checksum-final`, and `results/tus-node-task5-checksum-final`; all status files returned `0`. Probe path was included in `all-*` and filtered to `unsupported-*` when `checksum-trailer` was not advertised. |
| Concatenation edge behavior | Concatenation responses and HEAD expose `Upload-Concat`; final uploads are immutable and do not inherit partial metadata. | Header echo, final no-mutation, and metadata transfer edge cases are incomplete. | `tests/extensions/concatenation/ext-concat-001-create-partial.hurl`, `tests/extensions/concatenation/ext-concat-002-create-final.hurl`, `tests/extensions/concatenation/ext-concat-005-patch-final-forbidden.hurl`, `tests/extensions/concatenation/ext-concat-011-final-head-exact-concat.hurl`, `tests/extensions/concatenation/ext-concat-012-final-does-not-inherit-partial-metadata.hurl` | Dagger matrix exported to `results/tusd-task6-concat-term-final`, `results/rustus-task6-concat-term-final`, and `results/tus-node-task6-concat-term-final`; all status files returned `0`. TUSD POST response-header failures are source-verified skips; new HEAD/no-mutation checks remained active for TUSD. RUSTUS and tus-node-server filtered concatenation files as unsupported. |
| Termination follow-up semantics | Deleted uploads should remain unavailable to later requests. | PATCH-after-DELETE behavior is not covered. | `tests/extensions/termination/ext-term-003-deleted-404-410.hurl` | Dagger matrix exported to `results/tusd-task6-concat-term-final`, `results/rustus-task6-concat-term-final`, and `results/tus-node-task6-concat-term-final`; all status files returned `0`. TUSD ran the extended termination test active; RUSTUS and tus-node-server filtered termination files as unsupported. |
| Expiration lifecycle | Expiration responses use valid `Upload-Expires`; expired unfinished uploads become unavailable. | Existing Hurl tests are smoke coverage, not lifecycle verification. | `tests/extensions/expiration/ext-expire-001-post-response.hurl`, `tests/extensions/expiration/ext-expire-002-patch-response.hurl`, `tests/probes/expiration_lifecycle.py`; tus-node-server configured with short FileStore expiration in `servers/Dockerfile.tus-node-server` and `dagger.dang` only when expiration tests are selected. | Dagger matrix exported to `results/tusd-expiration-strict`, `results/rustus-expiration-strict`, and `results/tus-node-expiration-strict`; all status files returned `0`. TUSD and RUSTUS filtered expiration files and probe as unsupported; tus-node-server ran expiration tests and probe active. |
